apiVersion: v1
kind: Pod
metadata:
  name: readiness-demo
  labels:
    app: ready
spec:
  containers:
  - name: nginx
    image: nginx
    readinessProbe:
      httpGet:
        path: /
        port: 80
      periodSeconds: 5    
