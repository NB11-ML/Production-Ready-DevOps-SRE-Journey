apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
  labels:
    app: nginx
spec:
  clusterIP: None       # <-- This makes it "Headless"
  selector:
    app: nginx
  ports:
  - port: 80
    name: web